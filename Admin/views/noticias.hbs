<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/gamy.css">
    <!--Tipo de letra con Google Fonts-->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@300..700&family=Quicksand:wght@300..700&display=swap" rel="stylesheet">    
    <style>
        body {
            font-family: 'Quicksand', sans-serif;
        }
        *{
            padding: 30px;
            margin: 0;
            text-decoration: none;
            list-style: none;
            box-sizing: border-box;
        }
    </style>
    <title>Noticias</title>
</head>
<body>
    
<h1>Noticias</h1>
<a href="/nuevaPubli" class="btn-agr-not">Crear Noticia</a>
<br>

{{#if reports.length}}
<br>
  <table border="1" >
    <thead>
      <tr>
        <th>Título</th>
        <th>Información</th>
        <th>Imagen</th>
        <th>Acciones</th>
      </tr>
    </thead>
    <tbody>
      {{#each reports}}
        <tr>
          <td>{{this.titulo}}</td>
          <td>{{this.informacion}}</td>
          <td><img src="/imagen/{{this.id}}" alt="{{this.titulo}}"></td>
          <td>
            <a href="/editarNoticia/{{this.id}}" class="btn btn-warning">Editar</a>
            <a href="/eliminarNoticia/{{this.id}}" class="btn btn-danger">Eliminar</a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  <a href="/home">Volver</a>
{{else}}
  <p>No hay noticias disponibles.</p>
{{/if}}

</body>
</html>
